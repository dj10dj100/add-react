const statelessComponent = answers => {

    const {

    } = answers;



};

const classComponent = answers => {
    const {
        
    } = answers;
};

module.exports = {
    classComponent,
    statelessComponent
};